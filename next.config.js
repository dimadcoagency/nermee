/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable PWA-ready configuration
  // next-pwa will be added in Sprint 5 polish phase
  experimental: {},
};

module.exports = nextConfig;
